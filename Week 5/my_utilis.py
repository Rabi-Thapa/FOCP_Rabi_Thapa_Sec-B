def average(values):
    """Calculate the average of the given list."""
    total=0
    for n in values:
        total += float(n)

    return total/len(values)

# initialisation statment
print("Welcome, utilis module has been imported and initialised.")

